import { RoutePlaceholder } from "@/components/RoutePlaceholder";

export default function SignInPage() {
  return (
    <RoutePlaceholder
      title="Sign in"
      description="Authentication is deferred to identity tasks. This page keeps the header Sign in link valid."
    />
  );
}
