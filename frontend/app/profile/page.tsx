import { RoutePlaceholder } from "@/components/RoutePlaceholder";

export default function ProfilePage() {
  return (
    <RoutePlaceholder
      title="Profile"
      description="Profile management arrives with identity work. Placeholder keeps signed-in header navigation safe."
    />
  );
}
