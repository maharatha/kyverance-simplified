import { RoutePlaceholder } from "@/components/RoutePlaceholder";

export default function LatestBriefPage() {
  return (
    <RoutePlaceholder
      title="Agent brief"
      description="Full agent brief detail is deferred. Home shows a typed fixture summary only."
    />
  );
}
