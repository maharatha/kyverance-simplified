import { RoutePlaceholder } from "@/components/RoutePlaceholder";

export default function AgentsPage() {
  return (
    <RoutePlaceholder
      title="Agents"
      description="Agent registry and briefs will live here. Linked from Home as a safe placeholder."
    />
  );
}
