import { RoutePlaceholder } from "@/components/RoutePlaceholder";

export default function NewPortfolioPage() {
  return (
    <RoutePlaceholder
      title="Create portfolio"
      description="Portfolio creation arrives with the simulation domain. This route exists so Home CTAs do not 404."
    />
  );
}
