import { RoutePlaceholder } from "@/components/RoutePlaceholder";

export default function ExplorePage() {
  return (
    <RoutePlaceholder
      title="Explore"
      description="Curated public portfolios will live here. This placeholder keeps home navigation safe until Explore ships."
    />
  );
}
